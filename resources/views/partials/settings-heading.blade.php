<div class="flex flex-col gap-4 mb-6 sm:mb-8">
    <div class="text-center sm:text-left">
        <h1 class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white">{{ __('Settings') }}</h1>
        <p class="text-gray-600 dark:text-gray-400 mt-1 text-sm sm:text-base">{{ __('Manage your profile and account settings') }}</p>
    </div>
</div>
